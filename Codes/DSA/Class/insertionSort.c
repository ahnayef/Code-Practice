#include <stdio.h>

int insertionSort(int arr[], int len)
{

    // int arr[5] = {5, 3, 1, 2, 4};

    for (int i = 0; i < len; i++)
    {
        
    }

    for (int i = 0; i < len; i++)
    {
        printf("%d", arr[i]);
    }
}

int main()
{

    int arr[5] = {5, 3, 1, 2, 4};

    insertionSort(arr, 5);

    return 0;
}